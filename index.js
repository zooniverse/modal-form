if (typeof module !== 'undefined') {
  module.exports = {
    Dialog: require('./dialog'),
    Sticky: require('./sticky'),
    Anchored: require('./anchored'),
    Triggered: require('./triggered')
  };
}
